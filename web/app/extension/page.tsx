import ExtensionSurface from "@/components/extension/extension-surface"

/** Browser-extension surface. The local MV3 action opens this first-party route in a wallet-sized window. */
export default function ExtensionPage() {
  return <ExtensionSurface />
}
